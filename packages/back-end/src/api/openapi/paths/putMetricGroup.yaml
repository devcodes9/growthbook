parameters:
  - in: path
    name: id
    schema:
      type: string
    required: true
    description: Metric group id
tags:
  - metric-groups
summary: Update a metric group
operationId: putMetricGroup
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: "../payload-schemas/PutMetricGroupPayload.yaml"
responses:
  "200":
    content:
      application/json:
        schema:
          type: object
          required:
            - metricGroup
          properties:
            metricGroup:
              $ref: "../schemas/MetricGroup.yaml"
